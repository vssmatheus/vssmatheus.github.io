import React from 'react';
import PropTypes from 'prop-types';

const Home = () => {

  return (
    <>
      <h2>{'Home -> Página em Construção ;)'}</h2>
    </>
  );
};

Home.propTypes = {
  routes: PropTypes.array,
};

export default Home;