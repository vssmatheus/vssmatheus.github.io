import React from 'react';
import PropTypes from 'prop-types';

const Home = () => {

  return (
    <>
      <h2>This is Home</h2>
    </>
  );
};

Home.propTypes = {
  routes: PropTypes.array,
};

export default Home;