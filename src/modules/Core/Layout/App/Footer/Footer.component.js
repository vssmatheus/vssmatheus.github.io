import React from 'react';
import PropTypes from 'prop-types';

const Footer = () => {

  return (
    <>
      <h2>Footer</h2>
    </>
  );
};

Footer.propTypes = {
  routes: PropTypes.array,
};

export default Footer;